/** @format */

export class UserContextInfo {
   constructor(firstName, lastName, email, mobileNumber, role) {
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
      this.mobileNumber = mobileNumber;
      this.role = role;
   }
}

export const UserContextInfoList = new Array({});
